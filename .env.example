# Elasticsearch connection
ELASTICSEARCH_URL='https://your-project.es.us-central1.gcp.cloud.es.io'
ELASTICSEARCH_API_KEY='your-elasticsearch-api-key'

# Kibana connection (for Agent Builder)
KIBANA_URL='https://your-project.kb.us-central1.gcp.cloud.es.io'

# Option 1: Basic auth (most reliable for Kibana APIs)
# KIBANA_USERNAME='elastic'
# KIBANA_PASSWORD='your-password'

# Option 2: API key (works on serverless Elastic Cloud)
KIBANA_API_KEY='your-kibana-api-key'

# GitHub Personal Access Token (optional, but recommended to avoid rate limits)
GITHUB_PAT='ghp_your_github_token'
